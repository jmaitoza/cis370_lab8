// Joshua Maitoza
#include <stdio.h>
#include <unistd.h>

#define MAX_LINE 128 // max length of user command

int main(void) {
  while (1) {
    printf("WhatTheShell> "); // Name of shell
    fflush(stdout);
  }
}
